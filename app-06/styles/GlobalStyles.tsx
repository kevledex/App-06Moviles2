import { StyleSheet, Text, View } from 'react-native';

export const styleGlobal = StyleSheet.create({
    input: {
        borderWidth: 1,
        borderRadius: 10,
        margin: 6,
        padding: 10,
    },
    linea: {
        margin: 5,
        borderWidth: 5,
        borderColor: '#000000'
        
    }

})