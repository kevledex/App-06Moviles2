import { StyleSheet, Text, View } from 'react-native';

export const styleGlobal = StyleSheet.create({
    input : {
        borderWidth: 1,
        borderRadius: 10,
        margin: 5,
        padding: 10
    }
    
})